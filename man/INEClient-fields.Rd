% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/INEClient-fields.R
\name{INEClient-fields}
\alias{INEClient-fields}
\title{INEClient configuration fields}
\arguments{
\item{lang}{Character. Language code: \code{"PT"} (default) or \code{"EN"}.
Affects API responses, cache file paths, and display labels.}

\item{use_cache}{Logical. Whether to cache API responses locally.
Default \code{FALSE}.}

\item{cache_dir}{Character or \code{NULL}. Cache directory path.
If \code{NULL} (default), uses \code{tools::R_user_dir("ineptr2", "cache")}.}

\item{row_limit}{Integer. Maximum output rows per API request
before splitting into chunks. Must be between 1 and 1 000 000
(the API ceiling). Default \code{1000000L}.}

\item{max_retries}{Integer. Maximum retry attempts for failed
chunk downloads. Default \code{3L}.}

\item{progress_interval}{Integer. Print a progress message every
N chunks during downloads. Default \code{10L}.}

\item{timeout}{Numeric. Timeout in seconds for API requests
(metadata and data endpoints). Default \code{300} (5 minutes).
The catalog endpoint uses a separate, longer timeout.}
}
\description{
Configuration fields for the \link{INEClient} class. All fields are
implemented as active bindings with validation. Set them with
\code{ine$field <- value} and read them with \code{ine$field}.
}
\examples{
ine <- INEClient$new()
ine$lang
ine$lang <- "EN"

ine$use_cache <- TRUE
ine$cache_dir <- tempdir()
ine$row_limit <- 500000L

}
\seealso{
\link{INEClient} for methods.
}
